CREATE TABLE users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR (30), 
  pass VARCHAR (30), 
  age INT, 
  sex VARCHAR (30), 
  country VARCHAR (30), 
  state VARCHAR (30), 
  birthday VARCHAR(30)
)